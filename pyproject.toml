[project]
name = "gcp-data-pipeline-demo"
version = "0.1.0"
description = "Public BigQuery, Dataform, and Cloud Composer pipeline example"
requires-python = ">=3.11"

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
